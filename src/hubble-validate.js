import { sketchAlert } from './utils/sketchConfig';

export default function(context) {
  // TODO: check document config, check primitives page, check artboard name

  try {
    return sketchAlert(`🤚 `);
  } catch (error) {
    return sketchAlert(`🤚 `);
  }

  return sketchAlert(`🤚 `);
}
